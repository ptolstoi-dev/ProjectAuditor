@inherits LayoutComponentBase

<div class="flex flex-col md:flex-row min-h-screen bg-gray-50">
    <div class="w-full md:w-64 flex-shrink-0 bg-gray-900 md:min-h-screen">
        <NavMenu />
    </div>

    <main class="flex-1 overflow-y-auto">
        <div class="p-6 md:p-12 lg:p-16 max-w-full">
            @Body
        </div>
    </main>
</div>